//var moment: any = require('moment-timezone');

// a voir comment parser pour check a certaine h precise

export namespace Clock {
    /*
    export var getDateAmerica = async (params: any) => {
        let time = moment().tz('America/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    export var getDateAsia = async (params: any) => {
        let time = moment().tz('Asia/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    export var getDateAustralia = async (params: any) => {
        let time = moment().tz('Australia/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    export var getDateEurope = async (params: any) => {
        let time = moment().tz('Europe/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    export var getDateAfrica = async (params: any) => {
        let time = moment().tz('Africa/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    export var getDateArctic = async (params: any) => {
        let time = moment().tz('Arctic/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    export var getDateAntartica = async (params: any) => {
        let time = moment().tz('Antartica/' + params.country).format();
        console.log(time);
        return ({"result": time});
    };
    */
}